import React from 'react';
import { shallow } from 'enzyme';
import Chart from './Chart';

const props = {};

test('replace with useful test name', () => {
  const view = shallow(<Chart {...props} />);
  // Add a useful assertion here.
  expect(view).toBe(false);
});
